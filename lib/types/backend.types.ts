// import { Hono } from "hono";

// const booksRoutes = new Hono().get("/", (c) => c.json({ message: "" }));

// const app = new Hono();
// const routes = app.route("/books", booksRoutes);

// export type AppType = typeof routes;
